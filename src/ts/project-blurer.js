// let selected;

// addEventListener("DOMContentLoaded", () => {
//     const projects = document.getElementsByClassName("project");
//     const holders = document.getElementsByClassName("background");
    
//     // holders[0].style.borderRadius = "1em 0 0 1em";
//     // holders[holders.length-1].style.borderRadius = "0 1em 1em 0";

//     // projects[0].style.borderRadius = "1em 0 0 1em";
//     // projects[projects.length-1].style.borderRadius = "0 1em 1em 0";

//     for (const p of projects) {
//         p.addEventListener("mouseover", () =>{
 
//             console.log(p);

//             for(const t of projects){
//                 // if(t !== p) t.style.backdropFilter = "blur(.3em)";

//             }
//         });

//         p.addEventListener("mouseout", () =>{
//             for(const t of projects){
//                 if(t !== p) t.style.backdropFilter = "blur(0em)";

//             }
//         });
//     }
// });